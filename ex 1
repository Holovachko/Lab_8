import math
def calc(x,y):
    if x > y:
        return x**3 + math.fabs(x**2 + x**4)

    else:
        return (x**2 - 2 * x + math.fabs(x))/x**(3/5)
a = float(input('a = '))
b = float(input('b = '))
u = calc(a,b) + calc(2,a) + 2
print('U = {}'.format(u))
